# Copy to .env and set as needed

# OpenAI API key (optional, for AI image generation)
# If not set, the script will fall back to a generic random wallpaper.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1

# Google Gemini/Imagen API key (for image generation)
GEMINI_API_KEY=
# Optional override; otherwise set in image.config.json
GEMINI_MODEL=

# Desktop environment: cinnamon | gnome
# Defaults to cinnamon on Linux Mint
DESKTOP_ENV=cinnamon

# Output directory for generated wallpapers
OUTPUT_DIR=output
